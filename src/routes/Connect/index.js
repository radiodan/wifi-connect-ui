import { Component } from 'preact';

export default class Connect extends Component {
  render() {
    return (
      <div>
        <h1>Connecting</h1>
        <p>
          Radiodan is now connecting. This page will automatically close when
          the process is done.
        </p>
      </div>
    );
  }
}
